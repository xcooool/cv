# Protected CV

Static GitHub Pages version of `cv.html`.

To change the password, replace `PASSWORD_HASH` in `index.html` and `cv.html` with the SHA-256 hash of the new password.

Translations live in `i18n.js` (used by `index.html` only).
